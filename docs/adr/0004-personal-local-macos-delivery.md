# Keep the personal macOS build local

**Status: accepted.** The first release runs only on the owner's Apple Silicon Mac running macOS 14 Sonoma or later. It is built locally from this repository and replaced through an explicit local rebuild when the source changes. There is no Developer ID certificate, notarization, GitHub DMG distribution or automated application updater. GitHub remains source control, not a product delivery service.
