{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the local workbench window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
